name: CI

on:
    push:
        tags:
        - 'v*'

jobs:
    build:

        runs-on: ubuntu-18.04

        steps:
        - uses: actions/checkout@v1
        - name: Install prereqs
          run: ./install-prereqs.sh
        - name: Execute packer
          run: ./build.sh
        - name: Upload qcow2
          uses: actions/upload-artifact@v1
          with:
              name: qemu-kube-v1.16.2.qcow2
              path: ./output/ubuntu-1910-kube-v1.16.2/qemu-kube-v1.16.2.qcow2
        - name: Upload vmdk
          uses: actions/upload-artifact@v1
          with:
              name: qemu-kube-v1.16.2.vmdk
              path: ./output/ubuntu-1910-kube-v1.16.2/qemu-kube-v1.16.2.vmdk
        - name: Create Release
          id: create_release
          uses: actions/create-release@v1
          env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          with:
              tag_name: ${{ github.ref }}
              release_name: Release ${{ github.ref }}
              draft: true
              prerelease: true
        - name: Upload qcow2
          id: upload-release-asset-qcow2
          uses: actions/upload-release-asset@v1
          env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          with:
              upload_url: ${{ steps.create_release.outputs.upload_url }}
              asset_path: ./output/ubuntu-1910-kube-v1.16.2/qemu-kube-v1.16.2.qcow2
              asset_name: qemu-kube-v1.16.2.qcow2
              asset_content_type: application/zip
        - name: Upload vmdk
          id: upload-release-asset-vmdk
          uses: actions/upload-release-asset@v1
          env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          with:
              upload_url: ${{ steps.create_release.outputs.upload_url }}
              asset_path: ./output/ubuntu-1910-kube-v1.16.2/qemu-kube-v1.16.2.vmdk
              asset_name: qemu-kube-v1.16.2.vmdk
              asset_content_type: application/zip
